%!TEX root = ../main.tex

\section*{Supplementary Material}

  \renewcommand{\thefigure}{S\arabic{figure}}
  \setcounter{figure}{0}

  \renewcommand{\thetable}{S\arabic{table}}
  \setcounter{table}{0}

% ============================================================
%   SUPPLEMENTARY TEXT
% ============================================================

\subsection*{Supplementary Text S1: Dataset Descriptions}

We obtained and harmonised four published binary carbon source utilisation datasets spanning phylogenetically and ecologically diverse bacterial communities.
Each dataset captures growth outcomes (growth vs.\ no-growth) of bacterial strains on various carbon substrates.
Due to annotation pipeline limitations and incomplete phenotypic data, the number of genomes used in analyses was lower than the original dataset sizes (Table~\ref{tab:dataset_overview}).

\paragraph*{ATLeaf.}
The ATLeaf dataset~\cite{Schafer2023-nk} comprised 224 strains isolated from \textit{Arabidopsis thaliana} leaf surfaces, tested on 45 carbon substrates using minimal media on agar plates.
After genome annotation and quality filtering, 206 genomes were retained.
This dataset provided the broadest ecological coverage of phyllosphere-associated bacteria and served as one of three training/testing datasets.

\paragraph*{Biolog.}
The Biolog dataset encompassed 362 strains across 64 substrates, compiled from multiple published studies~\cite{BiologDatasetCitation}.
% TODO(CITATION): Ask Chris for the correct citation for this compiled Biolog phenotype matrix.
Growth phenotypes were measured using Biolog Phenotype MicroArray (PM) technology, which assesses carbon source utilisation via colorimetric tetrazolium-based readout in 96-well plates.
All 362 genomes passed quality control and were retained for downstream analysis.
The standardised, high-throughput nature of PM plates made this dataset a valuable complement to the other growth assays.

\paragraph*{Marine.}
The Marine dataset~\cite{Gralka2023-ft} contained 175 heterotrophic marine bacteria assessed on 100 carbon substrates using carbon source utilisation assays.
All 172 genomes that passed annotation quality control were retained for analysis.
This dataset offered taxonomic diversity distinct from the terrestrial datasets, with representation of marine-adapted lineages.

\paragraph*{Populus.}
The Populus source study profiled \textit{Pseudomonas fluorescens} isolates from \textit{Populus deltoides} rhizosphere and endosphere on Biolog PM plates~\cite{Timm2015Populus}; the compiled matrix used here included 58 isolates tested on 191 carbon sources.
% TODO(CITATION/PROVENANCE): Verify the source for the compiled 58-isolate Populus matrix; Timm et al. 2015 supports the Populus-associated phenotyping source but not this exact compiled count.
After filtering, 55 genomes were retained.
Due to its small sample size and narrow phylogenetic breadth, Populus contributed less statistical power than the other collections in some analyses and was excluded from selected feature-stability comparisons.

All analyses focused on the 15 shared phenotypes present across all four datasets.

\subsection*{Supplementary Text S2: Genome Quality Control}

We applied a multi-stage quality control pipeline to ensure that only high-quality, non-redundant genomes were used in downstream analyses.

\paragraph*{Assembly filtering.}
As a first filter, we excluded highly fragmented genome assemblies by removing genomes with more than 500 contigs.
This threshold retained well-assembled genomes while filtering out draft assemblies unlikely to yield reliable functional annotations.

\paragraph*{CheckM2 quality assessment.}
Remaining genomes were evaluated for assembly quality using CheckM2 \cite{Chklovski2023CheckM2}, which provides machine-learning-based estimates of genome completeness and contamination.
We retained genomes with completeness $>$90\% and contamination $<$5\%, consistent with community standards for high-quality genome assemblies.

\paragraph*{Outlier detection.}
We performed statistical outlier detection on the KEGG feature matrix using Isolation Forest \cite{Liu2008IsolationForest} with 100 estimators, contamination rate of 0.05, and random state 42.
This step identified genomes with anomalous functional profiles that could introduce noise into downstream machine learning models.

\paragraph*{Pangenome-based completeness assessment.}
As an independent quality metric, we estimated genome completeness based on the presence of species-specific core genes identified from pangenome analyses.
Core gene detection used MMseqs2 \cite{Steinegger2017MMseqs2} with a minimum sequence identity of 90\%, coverage of 80\%, and E-value threshold of $10^{-3}$.
Completeness was calculated as the fraction of species core genes detected in each genome (Figure~\ref{fig:pangenome_completeness}).
The majority of successfully assessed genomes exceeded 90\% pangenome completeness, corroborating the CheckM2-based filtering.

\paragraph*{Overall attrition.}
Across all datasets, the quality control pipeline retained 795 of 819 input genomes (97.1\%).
Per-dataset retention rates were: ATLeaf 206/224 (92.0\%), Biolog 362/362 (100.0\%), Marine 172/175 (98.3\%), and Populus 55/58 (94.8\%).
ATLeaf showed the highest absolute attrition, attributable to assembly fragmentation and CheckM2 quality filtering; the Biolog phenotype set passed quality control without further attrition because its constituent strains were curated upstream.

\subsection*{Supplementary Text S3: Model Selection and Evaluation Details}

We compared CatBoost gradient-boosted tree classifiers against Random Forest classifiers across dataset pairs and feature types (Table~\ref{tab:best_models_counts}).
In this table, ``cb'' denotes CatBoost and ``rf'' denotes Random Forest.
CatBoost was selected as the primary classifier because it achieved the best performance in the majority of train--test dataset and feature type combinations (45 out of 64 comparisons for KOFAM features).
All models were evaluated using balanced accuracy as the primary reporting metric to account for class imbalance in phenotype data, where the proportion of positive (growth) and negative (no-growth) samples varied substantially across phenotypes and datasets.

The minority-class filter (Methods) prevents balanced-accuracy collapse on test cells with extreme class imbalance --- for example, the Populus concordant test set contained 36--37 growers and a single non-grower for some amino-acid phenotypes.
The minority count was computed against the appropriate test-set definition for each figure: the concordant subset for Figure~\ref{fig:concordant_analysis}A and the concordant-test points of Figure~\ref{fig:concordant_analysis}D, the discordant subset for the discordant-test points of Figure~\ref{fig:concordant_analysis}D, and the full labelled subset for Figures~\ref{fig:ml_performance}B, \ref{fig:concordant_analysis}C, \ref{fig:improvement_strategies}B--D, and~\ref{fig:full_held_out_test}.
Implementation is consolidated in \texttt{scripts/minority\_filter.py}.

\subsection*{Supplementary Text S4: Feature Comparison Tables}

Tables~\ref{tab:feature_comparison} and~\ref{tab:feature_comparison_concordant} present stable predictive features identified through SHAP (SHapley Additive exPlanations) analysis.
Stable features were defined as those ranked among the top 10 by mean absolute SHAP value and appearing in at least 70\% of 20 random-seed training runs.
To avoid counting biologically redundant KOs (paralogues, alternative enzymes in one pathway) as distinct features, the union of stable KOs per phenotype was further grouped into target-aware redundancy clusters using \texttt{shap.utils.hclust} on the pooled concordant feature matrix at a default cut-off of 0.5 (Methods); the ``Shared stable features'' column annotates KOs with a cluster label (\verb|[Cluster A]|, etc.) when they belong to the same cluster.
This pooled clustering step was used only for post hoc feature grouping and did not affect model training or prediction.
Table~\ref{tab:feature_comparison} reports stable features from models trained on all samples using KOFAM annotations as features.
Table~\ref{tab:feature_comparison_concordant} reports stable KOFAM features from models trained on GapMind-concordant samples only, after a broad CatBoost-importance screen to make SHAP analysis tractable in the larger KOFAM feature space.
The contrast between these tables illustrates that concordant training produces more consistent, mechanistically interpretable feature clusters across datasets.

% ============================================================
%   TABLES
% ============================================================

\begin{table}[h]
\centering
\small
\begin{tabularx}{\textwidth}{lrrrrX}
\hline
\textbf{Dataset} & \makecell[r]{\textbf{Original} \\ \textbf{Strains}} & \makecell[r]{\textbf{Original} \\ \textbf{Substrates}} & \makecell[r]{\textbf{Genomes} \\ \textbf{Used}} & \makecell[r]{\textbf{Shared} \\ \textbf{Phenotypes}} & \textbf{Growth Assay} \\
\hline
ATLeaf  & 224 & 45  & 206 & 15 & Agar plate (minimal media) \\
Biolog  & 362 & 64  & 362 & 15 & Biolog PM (colorimetric) \\
Marine  & 175 & 100 & 172 & 15 & Carbon source assay \\
Populus & 58  & 191 & 55  & 15 & Carbon source assay \\
\hline
\end{tabularx}
\caption{\textbf{Overview of carbon source utilisation datasets.}
``Original Strains'' and ``Original Substrates'' reflect the published dataset sizes.
``Genomes Used'' reflects the number of genomes remaining after the annotation pipeline and quality filtering (Supplementary Text S2).
The 15 shared phenotypes (carbon sources present across all four datasets) were retained for analysis.
All four datasets were used in the aggregated analyses and leave-one-dataset-out evaluations; Populus was excluded only from selected feature-stability comparisons because of its small sample size.}
\label{tab:dataset_overview}
\end{table}

\begin{table}[h]
\centering
\begin{tabular}{llllr}
\hline
\textbf{Train Dataset} & \textbf{Test Dataset} & \textbf{Feature Type} & \textbf{Best Model} & \textbf{Count} \\
\hline
ATLeaf & Biolog & KOFAM & CatBoost & 13 \\
ATLeaf & Biolog & KOFAM & Random Forest & 3  \\
ATLeaf & Biolog & RAST  & CatBoost & 11 \\
ATLeaf & Biolog & RAST  & Random Forest & 5  \\
Biolog & ATLeaf & KOFAM & CatBoost & 11 \\
Biolog & ATLeaf & KOFAM & Random Forest & 5  \\
Biolog & ATLeaf & RAST  & Random Forest & 9  \\
Biolog & ATLeaf & RAST  & CatBoost & 7  \\
\hline
\end{tabular}
\caption{\textbf{CatBoost vs.\ Random Forest performance comparison across dataset pairs and feature types.}
For each combination of training dataset, test dataset, and feature type, the table shows how many of the 15 shared phenotypes were best predicted by each classifier (measured by balanced accuracy).
CatBoost outperformed Random Forest in the majority of comparisons, particularly with KOFAM features (24 vs.\ 8 wins across both dataset orderings).
See Methods for classifier hyperparameters and training details.}
\label{tab:best_models_counts}
\end{table}

\input{sections/table_feature_comparison}
\input{sections/table_feature_comparison_concordant}

% ============================================================
%   FIGURES
% ============================================================

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figure_s1.png}
\caption{\textbf{Phylogenetic distribution of microbial genomes across datasets.}
Phylogenetic tree displaying the 819 input genomes from four datasets: ATLeaf (green), Biolog (blue), Marine (orange), and Populus (red).
The tree was constructed using the Genome Taxonomy Database (GTDB)~\cite{Parks2018GTDB} as a reference framework, with phylogenetic distances calculated using the ETE 3 toolkit~\cite{HuertaCepas2016ETE3}.
Branch colors indicate dataset membership.
The tree reveals distinct taxonomic compositions across datasets: the Marine dataset is enriched in Gammaproteobacteria and Alphaproteobacteria marine lineages, ATLeaf spans diverse phyllosphere-associated taxa, and Biolog covers a broad phylogenetic range compiled from multiple studies.
Populus dataset has a narrower phylogenetic range in comparison.}
\label{fig:phylogenetic_distribution}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figure_s2.png}
\caption{\textbf{Phylogenetic distance distributions across evaluation split types.}
Distribution of cophenetic phylogenetic distances between training and test samples under different evaluation scenarios.
Random holdout evaluations (left) draw training and test samples from the same phylogenetic distribution, resulting in small distances.
Cross-dataset splits (middle) increase distances by separating samples from different experimental contexts.
Out-of-clade splits (right) produce the largest distances by ensuring test samples come from phylogenetically distant clades relative to training data.
This progressive increase in phylogenetic distance between training and test sets mirrors the increasingly stringent generalisation tests applied in the main text (Figure~\ref{fig:ml_performance}).}
\label{fig:phylogenetic_distance_splits}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figure_s3.pdf}
\caption{\textbf{In-clade vs out-of-clade performance across the 15 shared phenotypes (full unfiltered training data).}
Balanced accuracy of CatBoost classifiers using KOFAM features across the 15 shared phenotypes and the four leave-one-dataset-out combinations.
Top: in-clade splits (test samples drawn from clades represented in the training set).
Bottom: out-of-clade splits (test samples drawn from clades not represented in training).
Clade definitions were derived by Agglomerative clustering of the held-out test set on the phylogenetic distance matrix using the same parameters as Figure~\ref{fig:ml_performance}C; in-clade samples cluster with at least two training samples, out-of-clade samples form clusters lacking training representation.
Phenotype--dataset combinations with fewer than 10 test samples or 10 minority-class samples were excluded; 45 of 120 combinations were excluded under this rule, including all 15 Populus-held-out out-of-clade combinations because Populus tips lie within clades represented in the other three training datasets.
Out-of-clade balanced accuracies were consistently lower than in-clade (mean 0.629 vs 0.696), confirming that random holdout evaluation overestimates generalisation by allowing phylogenetically similar samples to appear in both training and test sets.}
\label{fig:in_clade_out_clade_performance}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figure_s4.pdf}
\caption{\textbf{Pangenome-based genome completeness assessment.}
Completeness was estimated as the fraction of species core genes detected in each genome using MMseqs2 (90\% identity, 80\% coverage, E-value $10^{-3}$; see Supplementary Text S2 for full parameters).
(\textbf{A}) Status breakdown: counts of genomes with successful assessment, no species assignment, no core-gene set, or processing error.
(\textbf{B}) Distribution of completeness (\%) among successfully assessed genomes.
The majority of genomes exceeded 90\% pangenome completeness, consistent with the CheckM2-based quality filtering applied upstream.
(\textbf{C}) Completeness by species (top 15 by genome count; others grouped as ``Other'').
(\textbf{D}) Core genes present versus expected for successful genomes, with points colored by completeness; the dashed line indicates perfect agreement.}
\label{fig:pangenome_completeness}
\end{figure}

\subsection*{Supplementary Text S5: Concordant Training Under the KOFAM Feature Space}

In the main analysis, GapMind concordance was used only to select training samples, whereas KOFAM annotations provided the model feature space.
This design decouples the concordance criterion from the model input features and avoids a tautological comparison in which GapMind-derived features are used to recover GapMind-defined concordance.
To isolate the effect of concordant training, we compared KOFAM-based models trained on all samples with KOFAM-based models trained only on GapMind-concordant samples.

Figure~\ref{fig:kofam_concordant} shows that concordant training improves cross-dataset balanced accuracy on concordant test samples under the same KOFAM feature space used in the main analysis.
The parity plot (panel~A) indicates that the majority of phenotype--split combinations achieve higher balanced accuracy with concordant training when evaluation is restricted to concordant held-out samples.
Per-phenotype box plots (panel~B) confirm that this improvement is consistent across most phenotypes, supporting the interpretation that data coherence, rather than feature--criterion alignment, drives the generalisation gains reported in the main text.

For the GapMind-feature pipeline-ceiling reference (Figure~\ref{fig:concordant_analysis}A, red dashed lines), raw rather than correlation-filtered GapMind features were used because GapMind represents the same underlying gene under multiple per-phenotype prefixes (for example, the same amino-acid transporter appears as \texttt{Alanine-braC}, \texttt{Serine-braC}, etc.), so the standard 0.95 correlation filter consolidates these duplicates and unfairly depletes the per-phenotype feature space for some pathways.
Because concordant labels are by construction consistent with GapMind's pathway-completeness rule, the GapMind-feature setting establishes the balanced accuracy reachable by the pipeline when feature resolution matches the mechanism.

\begin{figure}[ht]
\centering
\includegraphics[width=0.90\textwidth]{figure_s5.pdf}
\caption{
\textbf{Concordant training improves cross-dataset generalisation under KOFAM features.}
GapMind concordance was used only for sample selection; KOFAM annotations served as the model feature space.
(\textbf{A}) Parity plot comparing cross-dataset balanced accuracy of concordant-trained vs.\ full-trained models (KOFAM features), evaluated on concordant test samples only.
Each point represents one phenotype--split combination; points above the diagonal indicate improved performance with concordant training.
(\textbf{B}) Per-phenotype grouped box plots of cross-dataset balanced accuracy on concordant test samples for full training (grey) and concordant training (blue), both using KOFAM features.
}
\label{fig:kofam_concordant}
\end{figure}

\subsection*{Supplementary Text S6: Learning Curves for All 15 Shared Phenotypes}

The main text presents learning curves for histidine (Figure~\ref{fig:model_sample_size_requirements}).
To assess whether the ``50--100 high-quality samples'' observation generalises beyond this well-characterised pathway, we extended the analysis to all 15 shared phenotypes using concordant training with KOFAM features.

Figure~\ref{fig:all_phenotype_heatmap} summarises cross-dataset balanced accuracy as a function of training sample size for all 15 phenotypes.
Across phenotypes, performance typically approached its maximum by 50--100 training samples.
Only a small minority required 200 samples to reach the saturation threshold, and no phenotype showed a new saturation point beyond 200 samples.
The estimated saturation point varied modestly across phenotypes, where saturation was defined as the smallest sample size at which mean balanced accuracy reached $\geq$90\% of the corresponding full-dataset value.

These results show that the 50--100 sample sufficiency reported for histidine is representative of most shared phenotypes in this concordant-training setting rather than an isolated best-case scenario.
The distribution of saturation points suggests that many traits with coherent genotype--phenotype mappings can be learned efficiently, with only modest gains beyond 200 samples even for slower-saturating phenotypes.

\begin{figure}[ht]
\centering
\includegraphics[width=0.85\textwidth]{figure_s6.pdf}
\caption{
\textbf{Cross-dataset performance by training sample size across all 15 shared phenotypes.}
Heatmap of mean balanced accuracy for concordant-trained models (KOFAM features) evaluated on the cross-dataset full test set.
Rows are phenotypes; columns are training sample sizes (50, 100, 200, 500, and the full concordant training set).
Most phenotypes approach their maximum cross-dataset performance by 50--100 samples, with little additional improvement beyond 200 samples.
}
\label{fig:all_phenotype_heatmap}
\end{figure}

\subsection*{Supplementary Text S7: Concordant-Trained Model Performance on the Full Held-Out Test Set}

Figure~\ref{fig:concordant_analysis}A reports concordant-trained model performance on held-out concordant samples; Figure~\ref{fig:concordant_analysis}D decomposes cross-dataset performance into concordant and discordant test-subset balanced accuracy.
The natural-composition full held-out test set (with concordant and discordant samples pooled) is shown in Figure~\ref{fig:full_held_out_test} for completeness, together with per-phenotype test-set composition.
The full-test balanced accuracy (median 0.68 across phenotype--held-out-dataset pairs; range 0.47--0.94) falls between the concordant-only (median 0.89) and discordant-only (median 0.34) values, as expected from the test-set composition reported in Panel~B; the residual error is dominated by false-positive-discordant samples for most phenotypes.

\begin{figure}[ht]
\centering
\includegraphics[width=0.95\textwidth]{figure_s7.pdf}
\caption{
\textbf{Concordant-trained model performance on the full held-out cross-dataset test set.}
(\textbf{A}) Per-phenotype balanced accuracy on the full held-out cross-dataset test set (concordant and discordant pooled), evaluated for each of the four leave-one-dataset-out combinations.
The dashed horizontal line indicates random performance (balanced accuracy~$=0.5$).
(\textbf{B}) Per-phenotype composition of the held-out test set, summed across the four leave-one-dataset-out combinations: GapMind concordant (blue), false-positive discordant (red), and false-negative discordant (orange).
}
\label{fig:full_held_out_test}
\end{figure}

\subsection*{Supplementary Text S8: SHAP-Supervised Redundancy Clustering Procedure}

The stable-feature comparisons in Figures~\ref{fig:misclassification_diagnosis}C and~\ref{fig:concordant_analysis}B report cluster-level rather than raw-KO intersections so that paralogues, operon-mates, and alternative enzymes representing the same biological signal are counted as one entity.
The grouping uses the SHAP package's \texttt{shap.utils.hclust} routine~\cite{Lundberg2017SHAP,Lundberg2020TreeSHAP}: a univariate gradient-boosted model is fit from each candidate KO to the phenotype, a pairwise distance of $1-R^{2}$ is computed between the resulting per-sample predictions, and the resulting distance matrix is single-linkage clustered with the default cutoff of 0.5 --- so two KOs are placed in the same cluster when they share more than 50\% of their univariate explanatory power with respect to the phenotype.
Clustering was performed on the pooled concordant feature matrix across all four datasets so that cluster identities are common to the full-data and concordant-data analyses.
This procedure was applied only after stable features had been identified; it did not influence model fitting, held-out predictions, or balanced-accuracy estimates.
For tractability and to avoid the variance-inflation that the early-stopping schedule introduces into per-feature SHAP attributions across the 20 random seeds, the stability analyses used a fixed 500-iteration CatBoost variant trained without early stopping; this matches the default-classifier performance to within the per-seed variance reported in the main text.

For three phenotypes (alanine, mannose, serine) the stable features picked by different concordant-trained models did not reach this target-redundancy threshold yet remained cross-correlated within the held-out dataset (e.g., for mannose, the ribose- and xylose-uptake transporter ATP-binding proteins K10441 and K10545 with $|r|=0.67$ in the held-out concordant ATLeaf samples).
Inspection of the underlying KOFAM annotations showed these features comprise a heterogeneous mixture of carbohydrate-utilisation and accessory genes rather than members of a shared mechanistic pathway, indicating that for these three phenotypes the concordant-trained model generalises through co-occurring substrate-utilisation markers characteristic of broad carbohydrate utilisers rather than through convergent mechanistic features --- a less specific mode of transfer that remains within the applicability domain of organisms sharing these broader genomic repertoires.

\subsection*{Supplementary Text S9: Reliability and Experiment-Prioritisation Details}

CatBoost probabilities were not post hoc calibrated, so prediction confidence ($\max(p,\,1-p)$) was used only as a ranking score for selective prediction rather than as an absolute probability of correctness; calibration was assessed with reliability diagrams and the expected calibration error over ten equal-width probability bins, pooled across phenotypes for each model.
Per-phenotype risk--coverage curves were generated by stratifying abstention by predicted class: within each phenotype and model, predictions were partitioned by the predicted label and at each coverage level the top fraction of most-confident class-1 predictions (highest $p$) and the top fraction of most-confident class-0 predictions (highest $1-p$) were retained in parallel before balanced accuracy was recomputed on the retained subset.
This class-stratified construction avoids the degenerate single-predicted-class subsets that arise on individual phenotypes when ranking by raw $\max(p,\,1-p)$ for class-skewed classifiers, which would collapse balanced accuracy on the retained subset to 0.5 regardless of accuracy on the discarded fraction.

Feature-space novelty of a held-out genome was defined as its mean Jaccard distance to the five nearest training genomes in KOFAM feature space, computed against the appropriate per-split training set.
For the retrospective experiment-prioritisation simulation, each held-out dataset was split into candidate and evaluation pools; 25 candidate genome labels were added to the concordant training set, the model was retrained, and the change in cross-dataset balanced accuracy was measured on the evaluation pool across three random seeds and the four held-out datasets.
Models in this simulation were trained with a reduced number of boosting iterations for computational efficiency.

\subsection*{Supplementary Text S10: Composite Confidence Score for Data Quality Filtering}

The composite confidence score used to identify ambiguous training samples (Figure~\ref{fig:improvement_strategies}B) combined three inputs:
\begin{itemize}
    \item Phylogenetic confidence ($w = 0.2$): agreement among $k = 3$ nearest phylogenetic neighbours.
    \item GapMind confidence ($w = 0.3$): pathway completeness score mapped to a six-level scale (0.0--1.0) based on the GapMind completeness category.
    \item Experimental outcome ($w = 0.5$): binary experimental phenotype.
\end{itemize}
The experimental-outcome component is used solely for training-set quality assessment and sample exclusion; it is never available to the model at inference, since feature matrices supplied to the classifier do not include the phenotype label.
Samples with composite scores between 0.4 and 0.6 were considered ambiguous and excluded from the filtered training set.

\subsection*{Supplementary Text S11: Per-Phenotype Detail for Concordant-Training Analysis}

\paragraph{GapMind-feature ceiling failures.}
Even GapMind-feature models did not reach the random-holdout ceiling on every cross-dataset cell.
Residual gaps reflected applicability-domain effects (minority-class-sparse held-out concordant sets, where balanced accuracy is mechanically constrained; per-dataset shifts in which transporter or permease step features fire, so held-out genomes present zero-imputed step features the model never trained on) rather than failures of the GapMind rule itself.

\paragraph{Glucose feature-resolution gap.}
The largest KOFAM-vs-GapMind feature gap was glucose, where the GapMind ceiling of 0.77 substantially exceeded the KOFAM cross-dataset balanced accuracy of 0.51.
The GapMind glucose pathway invokes organism-specific transporter and PTS-system identifiers (e.g., \verb|MFS-glucose|, \verb|SSS-glucose|, \verb|ptsG-crr|) that lack direct KOFAM equivalents.

\paragraph{Histidine off-pathway stable features.}
For histidine on ATLeaf, the full-data unique stable features included $\beta$-ketoadipate-pathway components (K01032 \textit{pcaJ} 3-oxoadipate CoA-transferase $\beta$-subunit; K02623 \textit{pcaR}-type LysR regulator) and a methionine-cycle enzyme (K00547 homocysteine S-methyltransferase), all off-pathway with respect to M00045 (histidine degradation); the concordant-trained unique features were predominantly histidine-pathway paralogues or alternative steps (K01458 N-formylglutamate deformylase, K05603 formimidoylglutamate deiminase, K01745 histidine ammonia-lyase).
The $\beta$-ketoadipate pathway is widely distributed in soil and rhizosphere lineages with broad catabolic repertoires, so the full-data off-pathway features likely reflect phylogenetic co-occurrence with metabolically versatile clades rather than a histidine-specific route.

\paragraph{Calibration and per-phenotype model-confidence detail.}
Both the full-data and concordant-trained models were overconfident under cross-dataset shift, with expected calibration error 0.12 and 0.18, respectively (Figure~\ref{fig:selective_prediction}B).
Confidence-based abstention raised retained-subset balanced accuracy for phenotypes the models already predict well, but not for those they do not (Figure~\ref{fig:selective_prediction}A): both models remained accurate for m-inositol (balanced accuracy near 0.80 at all coverage levels), the concordant model exceeded the full-data model for histidine, and neither model rose above 0.5 for glucose, whose predictions had collapsed toward a single class.
On the rescue side, both FN-rescue and FP-rescue rates remained below the model's marginal class-prediction rate, indicating partial anchoring to GapMind's failure mode through shared pathway-linked features; the per-error-type asymmetry rather than the absolute rescue level is therefore the load-bearing finding.
