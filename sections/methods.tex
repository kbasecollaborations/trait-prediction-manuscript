%!TEX root = ../main.tex

\section*{Methods}

\subsection*{Carbon Source Utilization Datasets}

We compiled four binary growth datasets capturing growth outcomes of phylogenetically and ecologically diverse bacterial strains across various carbon sources.
ATLeaf \cite{Schafer2023-nk} comprised 224 strains tested on 45 carbon substrates using minimal media on agar plates.
Biolog encompassed 362 strains across 64 substrates, compiled from published studies \cite{}.
Marine \cite{Gralka2023-ft} contained 175 marine heterotrophic bacteria assessed across 100 carbon substrates.
Populus included 58 bacterial isolates tested on 191 carbon sources.~\href{https://www.frontiersin.org/journals/microbiology/articles/10.3389/fmicb.2015.01118/full}
\todo{Add citations for Biolog and Populus datasets}

Due to annotation pipeline limitations and incomplete phenotypic data, the final datasets used for analysis contained fewer genomes and phenotypes than the original datasets (Supplementary Table Sx).
All analyses focused on the 15 shared phenotypes across all datasets.
We used ATLeaf, Biolog, and Marine datasets for model training and testing, reserving Populus for independent validation.
We standardized carbon source nomenclature across datasets using a phenotype name normalization protocol.

\subsection*{Genome Annotation}

We processed all bacterial genomes through a standardized annotation pipeline using three complementary tools.
RAST annotation provided functional predictions via the RAST server, with results parsed from JSON format to extract ontology terms (\texttt{ontology\_terms.SSO}).
KOFAM annotation used Kofamscan with the \texttt{exec\_annotation} script and \texttt{-f mapper} option, annotating each genome individually in parallel.
GapMind (Price et al., 2020) predicted metabolic pathway completeness for carbon utilization, with pathway scores ranging from 0 (incomplete) to 2 (high confidence).

\subsection*{Phylogenetic Tree Construction}

We constructed a comprehensive phylogenetic tree incorporating microorganisms from all phenotype datasets using the Genome Taxonomy Database (GTDB) \cite{} as a reference.
Phylogenetic distances were calculated using the ete3 toolkit \cite{}.

\subsection*{Baseline Predictors}

\subsubsection*{GapMind Mechanistic Predictions}

We evaluated GapMind as a mechanistic baseline predictor.
GapMind predicts growth from metabolic pathway completeness scores.
We tested two prediction thresholds: strict (requiring complete pathways) and permissive (allowing likely complete pathways).
Binary predictions were generated by applying these thresholds to GapMind's pathway completeness scores for each carbon source.

\subsubsection*{Phylogeny-Based Nearest Neighbor}

We implemented a phylogenetic nearest neighbor classifier that predicts phenotypes based on the $k$ closest relatives in the training data.
For each test genome, we identified the $k=3$ nearest neighbors by phylogenetic distance and assigned the phenotype based on majority vote (or mean, rounded to binary).
This approach leverages the documented association between metabolic phenotypes and phylogenetic relationships.

\subsubsection*{Null Models}

We implemented two additional null model strategies to establish performance baselines:
\begin{itemize}
    \item Identity: Assigns the predicted phenotype based on the most common class in the training data
    \item Bernoulli: Assigns phenotypes based on the class probability observed in the training data
\end{itemize}

\subsection*{Data Pre-processing and Feature Selection}

Feature representations from KOFAM and GapMind annotations were converted to binary presence/absence matrices (except GapMind pathway scores, which retained numeric values).
We implemented two-stage filtering on the combined dataset across all training data:
\begin{itemize}
    \item Variance filtering: Removed features with variance below 0.01 using sklearn's VarianceThreshold
    \item Correlation filtering: Removed highly correlated features (Spearman $\rho > 0.95$) using a graph-based approach that retains features with highest connectivity
\end{itemize}
KOFAM annotations yielded approximately 4000--5000 features per dataset after filtering; GapMind features yielded approximately 1000 features.
All filtering was performed on training data to prevent data leakage.

\subsection*{Machine Learning Classifiers}

We trained gradient-boosted tree classifiers using CatBoost \cite{}, which showed slightly better performance than RandomForest in preliminary comparisons.
CatBoost was configured with the following parameters: 1000 iterations, learning rate 0.03, tree depth 4, L2 regularization 15, random subspace method (10\% features per split), Bayesian bootstrap, and early stopping with patience of 50 iterations.
Training used an 80:20 train:validation split with early stopping based on validation set performance to minimize overfitting.
We used balanced accuracy as the primary performance metric to account for class imbalance in phenotype data.

\subsection*{Evaluation Strategies}

\subsubsection*{Cross-Validation and Cross-Dataset Testing}

We evaluated model performance using four scenarios:
\begin{itemize}
    \item Random cross-validation: Standard train/test splits within the aggregated dataset
    \item Cross-dataset evaluation: Training on three datasets, testing on the held-out fourth dataset
    \item In-clade cross-dataset: Cross-dataset evaluation restricted to phylogenetically similar samples
    \item Out-of-clade: Cross-dataset evaluation on phylogenetically distant samples
\end{itemize}
For cross-dataset testing, we mapped features from training data to test data, imputing missing features as zero values.

\subsubsection*{Phylogenetic Partitioning}

We implemented the out-of-clade partitioning algorithm described by \cite{Li2023-up}, which systematically separates training and test data based on phylogenetic relationships.
The algorithm selects test clades based on a target test set size range (20--30\% of samples) while ensuring phylogenetic separation from training data.
We supplemented this with manual class-level partitioning using Gammaproteobacteria and Alphaproteobacteria as out-of-clade test cases.

\subsection*{Concordant and Discordant Sample Analysis}

To assess whether ML models learn mechanistic features, we stratified samples based on agreement between GapMind predictions and experimental outcomes.
Concordant samples are those where GapMind predictions match experimental results (correct predictions by the mechanistic model).
Discordant samples are those where GapMind predictions contradict experimental results (false positives or false negatives).
We trained models on concordant samples only and evaluated performance on both concordant and discordant test sets to assess whether learned features generalize beyond the mechanistic predictor's coverage.

We note that using GapMind features to train models on GapMind-concordant samples is not circular: GapMind's pathway completeness score provides a single aggregate prediction per phenotype, whereas ML models must learn which specific genes among the full feature set (approximately 1000 features spanning all carbon utilization pathways) are predictive for each phenotype.
The concordance filter selects samples with clear genotype-phenotype relationships; the ML model must still identify the relevant features within those samples.
However, this approach may preferentially select samples where the canonical pathway is the actual mechanism, potentially excluding organisms using alternative metabolic routes (see Discussion).

\subsection*{Feature Importance Analysis}

We identified stable predictive features using SHAP (SHapley Additive exPlanations) analysis \cite{}.
For each phenotype and dataset configuration, we trained models across 20 random seeds, extracting the top 10 features by mean absolute SHAP value from each run.
Features appearing in at least 70\% of runs were designated as ``stable features.''
We compared stable features across datasets to assess whether models learn consistent mechanistic signatures or dataset-specific patterns.

\subsection*{Data Quality Filtering Strategies}

We explored alternative approaches to improve model generalization beyond concordant sample filtering.

\subsubsection*{Problematic Sample Identification}

We identified systematically problematic samples: microbes showing zero growth on all substrates, microbes showing universal growth despite incomplete GapMind pathways, and genomes with the highest per-genome misclassification rates across phenotypes.
\todo{Specify threshold used for identifying high-misclassification genomes}

\subsubsection*{Confidence-Based Filtering}

We developed a composite confidence score combining three information sources:
\begin{itemize}
    \item Phylogenetic confidence ($w=0.2$): Agreement among $k=3$ nearest phylogenetic neighbors
    \item GapMind confidence ($w=0.3$): Pathway completeness score from GapMind
    \item Experimental data ($w=0.5$): Binary experimental outcome
\end{itemize}
Samples with confidence scores between 0.4 and 0.6 were considered ambiguous and excluded from filtered training sets.
\todo{Clarify rationale for confidence weight selection}

\subsection*{Sample Size Requirements}

We assessed minimum training data requirements using learning curves.
For selected phenotypes, we trained models on subsets of training data (50, 100, 200, 500, and full samples) using stratified subsampling with 3 repeats per configuration.
Performance was evaluated across random, cross-dataset, and out-of-clade splits, and on full, concordant, and discordant test subsets.
