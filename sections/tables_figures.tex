%!TEX root = ../main.tex

%--------------------------------------------------------%
%   FIGURE AND TABLE LEGENDS (text only, no images)
%   Each legend uses \refstepcounter to advance the figure/table counter
%   and bind the \label to the resulting number; the figures themselves
%   appear on dedicated pages below with no \caption{} (and so do not
%   re-increment the counter).
%--------------------------------------------------------%

\clearpage
\section*{Figures and Tables}

\refstepcounter{figure}\label{fig:dataset_characteristics}%
\noindent\textbf{Figure \arabic{figure}: Microbial carbon utilisation prediction workflow.}
(A) Overview of data collection, genome annotation, and machine learning workflow.
We collected growth phenotypes (growth vs.\ no-growth) of microbial isolates across 242 carbon sources from four datasets (ATLeaf, Biolog, Marine, and Populus); analyses focused on the 15 carbon sources shared across all four datasets (panel B).
Genome sequences were annotated using RAST, KOFAM, and GapMind, and the resulting feature matrices were used in training the classifiers.
(B) Phenotypic distribution for the 15 shared carbon sources across the four datasets before genomic-feature identifier matching.
The bar plot displays sample counts and class distributions for each phenotype.
Solid bars indicate growth (positive) samples, and patterned bars indicate no-growth (negative) samples.
Class distributions varied across phenotypes and datasets.
(C) Summary of the analysis variants evaluated in this study, showing for each its training data, feature set, evaluation, per-phenotype balanced-accuracy range with the median marked, and a one-line finding.

\noindent\textbf{Alt text:} Panel A shows a workflow schematic linking microbes and carbon sources to a growth/no-growth matrix, genome annotation via KOFAM, GapMind, RAST, and classifier training, with a table listing genome and phenotype counts for ATLeaf, Biolog, Marine, and Populus datasets. Panel B presents a stacked bar plot of positive and negative sample counts per phenotype across the four datasets. Panel C is a table of five analysis variants, each with its training data, feature set, evaluation, and a balanced-accuracy interval on a common axis (0.4 to 1.0, with a dashed line at 0.5) with the median marked: random holdout (0.70-0.90, median 0.85); cross-dataset full-data (0.48-0.84, median 0.62); concordance filtering (0.51-0.82, median 0.67); feature combination (0.55-0.85, median 0.66); and feature filtering (0.56-0.85, median 0.73). A final column gives a one-line finding for each variant.
\par\medskip

\refstepcounter{figure}\label{fig:baseline_predictions}%
\noindent\textbf{Figure \arabic{figure}: Baseline mechanism- and phylogeny-based predictions show limited accuracy.}
(A) Bar plot showing balanced accuracy for GapMind predictions across 15 phenotypes.
GapMind predictions were compared with experimental outcomes using both strict (blue) and permissive (pink) pathway completeness thresholds.
Dashed lines show the respective mean values.
(B) Dot plot showing balanced accuracy across for phylogeny-based nearest neighbour (k=3) predictions across 15 phenotypes under two evaluation scenarios: random holdout (top panel) and out-of-clade split (bottom panel).
Circles and boxes indicate null model performance (Identity and Bernoulli baselines) and triangles indicate nearest-neighbour performance.

\noindent\textbf{Alt text:} Panel A displays a grouped bar plot of GapMind balanced accuracy for 15 phenotypes under strict and permissive thresholds, with dashed mean lines near 0.65 and 0.70. Panel B shows two dot plots of nearest neighbour, Identity, and Bernoulli balanced accuracy under random and out-of-clade splits, with values clustering near 0.5 in the lower panel.
\par\medskip

\refstepcounter{figure}\label{fig:ml_performance}%
\noindent\textbf{Figure \arabic{figure}: ML models excel within datasets but struggle to generalise across datasets.}
(A) Box plot showing balanced accuracy under repeated random holdout evaluation for gradient-boosted tree classifiers (using KOFAM features) across 15 phenotypes; each phenotype contributes five values (one per data-split random state 42--46; Methods).
The dotted line indicates the GapMind permissive-threshold baseline performance on the test set.
(B) Dot plot showing cross-dataset balanced accuracy when training on three datasets and testing on the fourth held-out dataset.
Colors represent different test datasets.
The dotted line indicates the GapMind permissive-threshold baseline (same threshold as panel A).
(C) Difference plot comparing model performance between regular cross-dataset splits and phylogenetically constrained in-clade cross-dataset splits.

\noindent\textbf{Alt text:} Panel A shows box plots of random holdout balanced accuracy per phenotype clustering between 0.7 and 0.9, above a dashed GapMind baseline. Panel B is a dot plot of cross-dataset accuracy coloured by held-out test set, scattered around the GapMind line. Panel C plots full minus in-clade accuracy differences centred near zero (mean minus 0.024).
\par\medskip

\refstepcounter{figure}\label{fig:misclassification_diagnosis}%
\noindent\textbf{Figure \arabic{figure}: Diagnosing GapMind misclassifications and ML generalisation failures.}
(A) Conceptual framework outlining potential causes of concordant samples (where GapMind matches experiment) and discordant samples (where GapMind contradicts experiment).
(B) Stacked bar plots showing per-phenotype (top) and per-dataset (bottom) misclassification patterns.
Each bar displays the proportions of true positives, true negatives, false positives (GapMind predicts ``complete pathway'' but no growth is observed), and false negatives (GapMind predicts ``incomplete pathway'' but growth is observed).
(C) Feature importance stability analysis using SHAP.
Stable features are KOs appearing in $\geq$70\% of model runs across 20 random seeds; these are then grouped into stable feature clusters so that paralogues and pathway-mates representing the same biological signal are counted as one cluster (Methods).
Top panel: number of stable feature clusters across cross validation runs when the model is trained on all four datasets combined.
Bottom panel: cross-dataset cluster stability comparing the stable feature clusters from the model trained on the held-out dataset alone to those from the model trained on the three other datasets combined.
Solid bars represent clusters shared between the held-out-alone and combined-of-three models; patterned bars represent clusters present only in the held-out-alone model.

\noindent\textbf{Alt text:} Three-panel diagnostic figure. (A) Two-by-two conceptual matrix mapping GapMind predictions against experimental outcomes, listing biological causes for true positives, false negatives, false positives, and true negatives. (B) Stacked bar plots of TP, TN, FP, FN counts across fifteen phenotypes and four datasets. (C) Bar plots showing stable SHAP feature cluster counts per phenotype, combined and split by dataset, with shared and unique clusters.
\par\medskip

\refstepcounter{figure}\label{fig:concordant_analysis}%
\noindent\textbf{Figure \arabic{figure}: Concordance-trained models learn mechanistic features, outperform GapMind under random evaluation, and define an applicability domain under cross-dataset evaluation.}
All panels show models trained on GapMind-concordant samples with KOFAM features (Methods).
(\textbf{A}) Balanced accuracy on held-out concordant samples, the easier subset with discordant cases excluded (panel~C shows the full held-out test), under random holdout (green line) and cross-dataset evaluation (blue boxes); red dashed lines mark the pipeline-ceiling reference obtained by retraining and predicting using GapMind step features (Methods).
(\textbf{B}) Cross-dataset stability of SHAP-supervised redundancy clusters between models trained on the three non-held-out datasets and on the held-out dataset alone.
Solid bars: shared clusters; patterned bars: held-out-only clusters.
(\textbf{C}) Concordance-trained ML versus GapMind balanced accuracy on the full sample held-out test set, one point per phenotype mean.
Green circles: random holdout; blue squares: cross-dataset.
Dashed diagonal marks parity; points above indicate ML outperforming GapMind.
Inset: paired Wilcoxon $p$-values across phenotypes.
(\textbf{D}) As panel C, with the cross-dataset test set decomposed into GapMind-concordant (purple circles) and discordant (orange squares) subsets.
GapMind balanced accuracy is fixed at 1 on the concordant subset and 0 on the discordant subset by construction, so each series forms a vertical strip.

\noindent\textbf{Alt text:} Four-panel figure. (A) Box plots of balanced accuracy across fifteen phenotypes comparing dataset-split KOFAM models against random-split and GapMind reference lines. (B) Stacked bars per phenotype showing common and unique stable feature clusters across AtLeaf, Biolog, and Marine datasets. (C) Scatter of concordant-ML versus GapMind balanced accuracy with random and cross-dataset markers near the diagonal. (D) Same scatter split into concordant points clustered at x equals one and discordant points at x equals zero.
\par\medskip

\refstepcounter{figure}\label{fig:improvement_strategies}%
\noindent\textbf{Figure \arabic{figure}: Effects of data filtering and feature selection on model performance.}
(A) Left: counts of problematic genomes per dataset in two discordant categories: no observed growth despite at least one GapMind growth prediction, and universal observed growth despite incomplete GapMind pathways.
Right: top-20 microbes ranked by number of phenotypes on which GapMind disagrees with the experimental label, coloured by membership in those categories from panel A.
(B) Cross-dataset balanced accuracy, precision, and recall for the soft-label confidence filter at $w_{\mathrm{gap}} \in \{0, 0.3, 0.4, 0.5\}$, with three reference conditions plotted at the left and right: no-filter ML and problematic-sample removal (left), and concordance training (right).
Large markers show the mean across the 15 phenotype-level values, error bars show the standard error of the mean across those values, the small dots are the per-phenotype values jittered horizontally to expose the distribution, and the mean train+val sample size per phenotype-split is annotated below each tick.
The $w_{\mathrm{gap}} = 0$ setting is the mechanism-free configuration; all conditions are evaluated on the same full held-out cross-dataset test set.
(C) Per-phenotype $\Delta$ recall (ML minus GapMind) on the same test set for ML models trained on concordance-filtered (blue) and mechanism-free filter (light green); phenotypes are sorted by concordant-ML $\Delta$ recall, and mean train+val sample sizes are given in the legend.
Annotation: count of phenotypes with positive $\Delta$ and paired Wilcoxon $p$-value.
(D) Balanced-accuracy scatter under random holdout and cross-dataset evaluation comparing combined (GapMind + KOFAM + RAST) and phenotype-filtered (GapMind-only) features; points above the diagonal (equal performance, dashed) indicate that phenotype-filtered features outperform the combined set.
The annotation reports split-specific paired Wilcoxon p-values across phenotype means.

\noindent\textbf{Alt text:} Four-panel figure. Panel A: two horizontal bar charts counting problematic genomes by dataset and top-20 microbes by misclassification number. Panel B: mean balanced accuracy, precision, and recall across the GapMind weight sweep of the confidence filter, with error bars showing the standard error of the mean and marker columns for no-filter ML and problematic-samples removed on the left and concordant training on the right. Panel C: per-phenotype $\Delta$ recall bars (ML minus GapMind) for concordance-trained ML and the mechanism-free confidence filter, sorted by the concordant-ML $\Delta$ recall, with an annotation reporting the count of phenotypes on which each ML filter exceeded GapMind and the paired Wilcoxon $p$-value. Panel D: balanced-accuracy scatter for phenotype-filtered versus combined features under random and cross-dataset splits.
\par\medskip

\refstepcounter{figure}\label{fig:selective_prediction}%
\noindent\textbf{Figure \arabic{figure}: Reliability of genome-based predictions and label-free prioritisation of follow-up experiments.}
(\textbf{A}) Risk--coverage curves for three phenotypes spanning high, intermediate, and low cross-dataset balanced accuracy (m-Inositol, Histidine, Glucose), comparing the full-data model (orange) and the concordance-trained model (blue).
Balanced accuracy is shown on the retained subset as a function of coverage, the fraction of held-out genomes the model commits to, with the least-confident genomes abstained on first; retention is stratified by predicted class so both predicted classes are represented at every coverage level.
The grey dashed line marks random performance (balanced accuracy~$=0.5$).
(\textbf{B}) Reliability diagram for both models on the cross-dataset held-out test set, pooled across phenotypes (10 equal-width probability bins): mean predicted probability of growth against the observed growth fraction within each confidence bin; the dashed diagonal marks perfect calibration and the expected calibration error (ECE) is given in the legend.
(\textbf{C}) Change in cross-dataset balanced accuracy after adding 25 held-out genome labels to the concordant training set, by label-free selection strategy (low confidence, diversity, random, high feature-novelty); violins show the distribution across 68 valid phenotype--held-out-dataset--seed runs (six phenotypes, up to four held-out datasets and three seeds per phenotype), and overlaid points show individual runs.
(\textbf{D}) Per-phenotype gain from 25 randomly versus low-confidence-selected added labels, for the three Panel-A phenotypes; boxes show the median and interquartile range, overlaid points show individual runs, and thin grey lines connect matched (held-out dataset, seed) runs across the two strategies ($n=12$ for m-Inositol, $n=12$ for Histidine, and $n=9$ for Glucose).
Distributions in Panels C and D are right-skewed (a small fraction of runs produce large positive gains), so the per-strategy means reported in the main text are larger than the medians shown in the figure.

\noindent\textbf{Alt text:} Four-panel figure. Panel A: three risk-coverage line plots for m-Inositol, Histidine, and Glucose, showing balanced accuracy declining or flat as coverage rises, comparing concordant and full-data models. Panel B: reliability diagram with both models tracking near diagonal. Panel C: violins with overlaid jittered points across four selection strategies. Panel D: side-by-side box plots per phenotype for random and low-confidence strategies, with thin grey lines connecting matched paired runs.
\par\medskip

\refstepcounter{table}\label{tab:main_feature_comparison}%
\noindent\textbf{Table \arabic{table}: Per-phenotype feature recovery under concordance filtering.}
For each phenotype, two models are compared: a three-dataset model trained on the three non-held-out source datasets pooled together, and a held-out-alone model trained on the held-out dataset alone.
This pairwise comparison is repeated with each of the three datasets in turn serving as the held-out one, and counts are summed across these three held-out comparisons (Methods).
The two columns labelled Full $\rightarrow$ Conc. report the same comparison under two training regimes applied to both models: Full uses all training genomes, while Conc. restricts training to GapMind-concordant samples.
The shared stable-cluster count comprises SHAP-supervised redundancy clusters present in both the three-dataset and held-out-alone models (the same quantity plotted in Figure~\ref{fig:concordant_analysis}B), summed across the three held-out comparisons.
The unique stable-cluster count comprises those present only in the held-out-alone model on the same basis.
The parenthetical (\% in pathway) is the fraction of those clusters whose membership includes at least one KO on the row's assigned KEGG reference pathway map (Methods); a cluster counts as pathway-resident when any of its KOs is on the map.
The final example column reports one or two representative shared concordant-stable features for that phenotype, selected automatically and rendered with their KEGG descriptive names.
Where the two models share a feature outright the shared KO is shown; where they share a redundancy cluster but not the identical KO (so the cell would otherwise be empty despite a non-zero shared-cluster count), a representative KO from each shared cluster is shown instead, keeping this column consistent with the shared-cluster count.
Features whose redundancy cluster is pathway-resident are preferred; off-pathway features such as transporters or regulators are shown only when no pathway-resident feature is stable, so the column still conveys the biological signal the model relies on (for Glycerol the stable features are the glpF glycerol facilitator and the glpR glycerol-3-phosphate regulon repressor, transport and regulation rather than catabolic enzymes; for Arginine the only shared signal is an off-pathway choline/betaine osmolyte cluster, a confounder rather than arginine catabolism).
This column is illustrative rather than exhaustive, and its feature selection is independent of the (\% in pathway) coverage statistic, which continues to score only canonical pathway-map residency.
Histidine is the worked example (numeric cells in bold); its per-comparison KO lists and the narrower unique-cluster module coverage (M00045 \textit{histidine degradation}: 29\% $\rightarrow$ 75\%) appear in Supplementary Table~\ref{tab:feature_comparison} for full-data and concordant training.

\noindent\textbf{Alt text:} Five-column table listing 15 phenotypes with their KEGG pathway map, shared stable cluster counts with parenthetical percent-in-pathway (Full to Conc.), unique stable cluster counts with parenthetical percent-in-pathway (Full to Conc.), and an example shared concordant-stable feature (enzyme, transporter, or regulator). Histidine row shows bold values rising from 4 (75 percent) to 8 (38 percent) shared clusters and 6 (17 percent) to 4 (50 percent) unique clusters.
\par\medskip

%--------------------------------------------------------%
%   FIGURES AND TABLES (images and table, each on its own page)
%   The \caption*{} (asterisked, no auto-numbering) just labels each
%   figure with its number for the reader; counters were already
%   advanced in the Legends section above so \ref{} resolves correctly.
%--------------------------------------------------------%

\clearpage

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth]{figure1a.pdf}\\[0.5cm]
\includegraphics[width=\textwidth]{figure1b.pdf}\\[0.15cm]
\includegraphics[width=\textwidth]{figure1c.pdf}
\caption*{\textbf{Figure 1}}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth]{figure2.pdf}
\caption*{\textbf{Figure 2}}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth]{figure3.pdf}
\caption*{\textbf{Figure 3}}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth]{figure4.pdf}
\caption*{\textbf{Figure 4}}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth]{figure5.pdf}
\caption*{\textbf{Figure 5}}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth]{figure6.pdf}
\caption*{\textbf{Figure 6}}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth]{figure7.pdf}
\caption*{\textbf{Figure 7}}
\end{figure}

\input{sections/table_main_feature_comparison.tex}
